<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.make_a_story_prototype">

    <!--for using glide-->
    <uses-permission android:name='android.permission.INTERNET' />

    <application
        android:hardwareAccelerated="false"
        android:largeHeap="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".main.LaunchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.Home.view.HomeActivity"/>
        <activity android:name=".main.Categories.view.CategoriesActivity"/>
        <activity android:name=".main.Characters.view.CharacterActivity"/>
        <activity android:name=".main.Quiz.view.QuizActivity"/>
        <activity android:name=".main.Wordbank.view.WordbankActivity"/>
        <activity android:name=".main.Quiz.view.CharacterGuideActivity"/>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>